---
apiVersion:
kind:
metadata:
  lables:
    app: backend
spec:
  replicas: 3
  selector:
    matchLable:
      app: backend
  template:
    metadata:
      app: backend
    spec:
      containers:
        - name:
